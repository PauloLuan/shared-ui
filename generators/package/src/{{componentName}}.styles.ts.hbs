import styled, { css } from 'styled-components'
import { default as Bootstrap{{pascalCase componentName}} } from 'react-bootstrap/{{pascalCase componentName}}'

// import { {{pascalCase componentName}}Props } from '.'

export const {{pascalCase componentName}} = styled(Bootstrap{{pascalCase componentName}})`
  ${() => css`
    display: flex;
  `}
`
