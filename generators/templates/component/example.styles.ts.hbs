import styled, { css } from 'styled-components'
import { default as Bootstrap{{pascalCase name}} } from 'react-bootstrap/{{pascalCase name}}'

// import { {{pascalCase name}}Props } from '.'

export const {{pascalCase name}} = styled(Bootstrap{{pascalCase name}})`
  ${() => css`
    display: flex;
  `}
`
